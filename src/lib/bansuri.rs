
use std::boxed::Box;
use std::io;

pub fn parse_js_reader(input_reader: Box<io::Read>, output_writer: Box<io::Write>) {

}

pub fn parse_js_buf(input_buf: Vec<u8>, output_buf:Vec<u8>) {
}

#[cfg(test)]
mod bansuriTests {
    #[test]
    fn parse_test() {
    }
}